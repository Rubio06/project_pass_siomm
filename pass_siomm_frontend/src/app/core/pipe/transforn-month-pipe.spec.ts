import { TransfornMonthPipe } from './transforn-month-pipe';

describe('TransfornMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new TransfornMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
